-- create table task_manager
-- (
--     task_id numeric(3) primary key,
--     user_id numeric(3),
--     title varchar(50),
--     description varchar(150),
--     due_date date,
--     status varchar(50)
-- );

-- create table user_data
-- (
--     user_id numeric(3) primary key,
--     name varchar(50),
--     designation varchar(20)
-- );

select *
from task_manager;

select *
from user_data;